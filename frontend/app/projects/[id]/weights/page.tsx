"use client";
import { FormEvent, useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { apiClientJson, WeightEntry } from "@/lib/api";
export default function ProjectWeightsPage() { const { id } = useParams<{ id: string }>(); const [rows, setRows] = useState<WeightEntry[]>([]); const load = async () => setRows(await apiClientJson<WeightEntry[]>(`/projects/${id}/weights`)); useEffect(() => { load().catch(() => undefined); }, [id]); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); await apiClientJson(`/projects/${id}/weights`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ recorded_at: form.get("recorded_at"), weight_lbs: Number(form.get("weight_lbs")), notes: form.get("notes") }) }); event.currentTarget.reset(); await load(); }; return <div className="space-y-3"><h1 className="text-2xl font-semibold">Weights</h1><form onSubmit={submit} className="grid gap-2 rounded border border-white/10 bg-neutral-900 p-3"><input name="recorded_at" type="date" defaultValue={new Date().toISOString().slice(0, 10)} className="rounded bg-neutral-800 p-2" /><input name="weight_lbs" type="number" step="0.1" required placeholder="Weight (lbs)" className="rounded bg-neutral-800 p-2" /><textarea name="notes" className="rounded bg-neutral-800 p-2" placeholder="Notes" /><button className="rounded bg-blue-700 px-3 py-2 text-sm">Add</button></form>{rows.map((r) => <div key={r.id} className="rounded border border-white/10 bg-neutral-900 p-3 text-sm">{r.recorded_at.slice(0, 10)} • {r.weight_lbs} lbs<br />{r.notes}</div>)}</div>; }
