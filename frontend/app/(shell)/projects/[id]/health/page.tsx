"use client";
import { FormEvent, useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { apiClientJson, HealthEntry } from "@/lib/api";
export default function ProjectHealthPage() { const { id } = useParams<{ id: string }>(); const [rows, setRows] = useState<HealthEntry[]>([]); const load = async () => setRows(await apiClientJson<HealthEntry[]>(`/projects/${id}/health`)); useEffect(() => { load().catch(() => undefined); }, [id]); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); await apiClientJson(`/projects/${id}/health`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ recorded_at: form.get("recorded_at"), category: form.get("category"), description: form.get("description"), cost: form.get("cost") || null, vendor: form.get("vendor") || null, attachment_receipt_url: form.get("attachment_receipt_url") || null }) }); event.currentTarget.reset(); await load(); }; return <div className="space-y-3"><h1 className="text-2xl font-semibold">Health</h1><form onSubmit={submit} className="grid gap-2 rounded border border-white/10 bg-neutral-900 p-3"><input name="recorded_at" type="date" defaultValue={new Date().toISOString().slice(0, 10)} className="rounded bg-neutral-800 p-2" /><select name="category" className="rounded bg-neutral-800 p-2"><option>treatment</option><option>vaccination</option><option>injury</option><option>note</option></select><textarea name="description" required className="rounded bg-neutral-800 p-2" placeholder="Description" /><input name="cost" type="number" step="0.01" placeholder="Cost" className="rounded bg-neutral-800 p-2" /><input name="vendor" placeholder="Vendor" className="rounded bg-neutral-800 p-2" /><input name="attachment_receipt_url" placeholder="Receipt URL" className="rounded bg-neutral-800 p-2" /><button className="rounded bg-blue-700 px-3 py-2 text-sm">Add</button></form>{rows.map((r) => <div key={r.id} className="rounded border border-white/10 bg-neutral-900 p-3 text-sm">{r.recorded_at.slice(0, 10)} • {r.category}<br />{r.description}</div>)}</div>; }
